services:
  - type: web
    name: calculadora-perda-chapas
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:${PORT:-10000} app:app
    healthCheckPath: /health
